---
galaxy_info:
  author: Dennis Witt
  description: Ansible role to setup a wireguard client device
  namespace: wittdennis
  role_name: wireguard_client
  license: MIT
  platforms:
    - name: Alpine
      versions:
        - all
    - name: Debian
      versions:
        - all
    - name: Rocky
      versions:
        - all
    - name: Ubuntu
      versions:
        - all

  min_ansible_version: "2.1"

  galaxy_tags:
    - wireguard
    - networking

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
